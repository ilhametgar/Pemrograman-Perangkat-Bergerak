<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="etgar.project.myintentactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="etgar.project.myintentactivity.HalamanActivity2"></activity>
        <activity android:name="etgar.project.myintentactivity.HalamanActivity1" />
        <activity android:name="etgar.project.myintentactivity.MoveActivityWithData" />
        <activity android:name="etgar.project.myintentactivity.NewActivity" />
        <activity android:name="etgar.project.myintentactivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>